# Port Configuration
APP_PORT=8080
APP_URL=http://localhost:8080
SERVER_PORT=3001
VITE_API_BASE_URL=http://localhost:3001
STORYBOOK_PORT=6006
STORYBOOK_URL=http://localhost:6006

# Model Configuration
# This can be overridden with --model flag in command line
# Options: gpt-4.1-nano, gpt-4o, gpt-3.5-turbo, claude-3-opus, claude-3-sonnet, claude-3-haiku, none
DEFAULT_MODEL=gpt-4.1-nano

# API Keys for different model providers
# Leave commented out the ones you're not using

# OpenAI API Key (Required for GPT models)
# OPENAI_API_KEY=sk-...

# OpenAI Admin API Key (Used for tracking usage)
# OPENAI_ADMIN_KEY=sk-...

# Anthropic API Key (Required for Claude models)
# ANTHROPIC_API_KEY=sk-ant-...

# Braintrust API Key (optional, for evals)
# BRAINTRUST_API_KEY=sk-...

# To run without any API key, use: 
# npm run start -- --model none
# or set DEFAULT_MODEL=none in your .env file

# Token for securing the internal Usage API endpoint
# USAGE_API_AUTH_TOKEN=secret_sauce
# Should only be used in storybook and not main app for security
# VITE_USAGE_API_AUTH_TOKEN=secret_sauce